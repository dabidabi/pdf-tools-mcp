[project]
name = "pdf-tools-mcp"
version = "0.2.0"
description = "Lightweight local PDF tools for MCP-compatible AI coding agents."
requires-python = ">=3.9"
dependencies = [
  "pypdf>=4.0.0,<6.0.0",
]

[tool.pdf-tools-mcp]
page_numbering = "1-based"
