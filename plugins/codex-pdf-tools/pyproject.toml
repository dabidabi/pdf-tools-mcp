[project]
name = "codex-pdf-tools"
version = "0.1.0"
description = "Lightweight local PDF tools for Codex."
requires-python = ">=3.9"
dependencies = [
  "pypdf>=4.0.0,<6.0.0",
]

[tool.codex-pdf-tools]
page_numbering = "1-based"
